что такое python?:
Синтаксис + идеалогия(принципы которые описывают как должен работать современный и понятный
код, эти принципы описываются в модуле this)

python не диктует как работает память, вопросы "как должен выглядить принт, фор" - это больше до CPython - конкретная
реализация. Есть и другие реализации - Jpython, Py-py, IronPython, MicroPython, Fast C Python - c 3.11 быстрее на 25%

Cpython
strongly type str + int - error. general purpose - для разных задач.
больше интерпретованый язык, но и создается байт код через С
динамически типизированный язык, что это, приведи пример - язык где переменные определеняются динамически, не нужно явно указывать тип переменной
как например в джаве



boolean может единицой, это динамически


литералы что это, где используем, куда можем записать - фиксированные значения, к-е могут сохраняться в переменную
и такие типы данных как

что такое keyboard in python - зарезервинованные слова, к-е не можно юзать как переменную

что такое флейк8, зачем и где юзается
какие есть типы данных(мут, анмут) - хешбл, анхешбл, в чем разница добавление в список и в переменную a
([1, 2, 3],) - можно ли тут что-то сделать чтоб id не изменился в кортеже

что такое lambda func, сколько параметров принимает, возвращает. Это анонимная функция
Что такое clousare
что такое области видимости
args, kwargs. В каких типах принимают. какие аргументы  - позиционные, по именам

list/dict compr - созданный для удобства. Данные может брать с итерируемого объекта

что такое декораторы и как они работают. Как убрать docstring

що таке итератор, генератор, в чем разница
статик метод разница класс метод. зачем cls(изменять переменные класса). Для чего юзать статикметод, почему не вынести за класс
обязательно нужно сказать что это декораторы

дескрипторы  - что это, зачем их юзать, какие методы
что такое ооп, их концепции
какие есть типы наследования
что такое мро(метод)
какой последний класс(обджект)
что такое полиморфизм - говори что концепция ооп - один метод, разная реализация
из какого модула импортируется абс
что такое абсракция - ментор ответил - пример не наводить сразу(так как может показаться что обсуждаешь инкапсуляцию).
Это объединение всех 3-х принци